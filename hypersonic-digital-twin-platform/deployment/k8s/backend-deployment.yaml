apiVersion: apps/v1
kind: Deployment
metadata:
  name: aether-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aether-backend
  template:
    metadata:
      labels:
        app: aether-backend
    spec:
      containers:
        - name: backend
          image: aether-twin/backend:0.1.0
          ports:
            - containerPort: 8000
          env:
            - name: SIMULATION_TICK_HZ
              value: "5"

